<div class="container">
  <h1><%= @list.name %></h1>
  <%= link_to "Add a movie", new_list_bookmark_path(@list) %>
  <ul>
    <% @list.movies.each do |movie| %>
      <li>
        <%= movie.title %> / <%= movie.overview %>
      </li>
    <% end %>
  </ul>
</div>
